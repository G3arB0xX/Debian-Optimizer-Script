# Podman 存储配置 — apps 用户
# 路径: /home/apps/.config/containers/storage.conf
# graphroot 置于 /home/apps/srv/storage，便于主用户通过 ACL 维护数据

[storage]
driver = "overlay"
graphroot = "/home/apps/srv/storage"
runroot = "/run/user/{{APPS_UID}}/containers"

{{FUSE_OVERLAY_SECTION}}
