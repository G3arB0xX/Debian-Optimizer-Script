# DNS API credentials for Lego
export CLOUDFLARE_DNS_API_TOKEN="{{CF_TOKEN}}"

# Debopti metadata
export DEBOPTI_DOMAINS="{{DOMAINS}}"
export DEBOPTI_EMAIL="{{EMAIL}}"
export DEBOPTI_PROVIDER="{{PROVIDER}}"
export DEBOPTI_AUTO_RENEW="{{AUTO_RENEW}}"
export DEBOPTI_FERRON_PUSH="{{FERRON_PUSH}}"
export DEBOPTI_DNS_SKIP_PROPAGATION="{{DNS_SKIP_PROPAGATION}}"
